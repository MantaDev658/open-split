services:
  postgres:
    image: postgres:15-alpine
    container_name: opensplit-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: opensplit
    ports:
      - "5432:5432"
    volumes:
      - opensplit-data:/var/lib/postgresql/data

volumes:
  opensplit-data: